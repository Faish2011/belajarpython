import sys

arg_list = sys.argv

arg_len = len (arg_list)

ord_no = 1

print (f"arguments captured = {arg_len}")

for arg in arg_list:
    print (f"argument {ord_no}: {arg}")
    ord_no += 1


































